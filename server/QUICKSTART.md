"""Quick start guide for IFC Processing Server

SETUP:
1. Install dependencies: pip install -r requirements.txt

RUNNING:
Option A - Python script:
    python ifcxServerFileStore.py

Option B - Direct Flask:
    python app.py

Option C - VS Code Debug:
    Press F5 or Run → Start Debugging
    Select "Flask Server" from dropdown

ACCESSING:
    Admin: http://localhost:5000
    API: http://localhost:5000/api/

UPLOADING FILES:
    - Drag & drop .ifc or .json files
    - Files processed automatically
    - Components stored in dataStores/fileBased/data/

STOPPING:
    Press Ctrl+C in terminal
"""
